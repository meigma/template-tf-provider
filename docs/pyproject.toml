[project]
name = "template-go-docs"
version = "0.0.0"
description = "MkDocs documentation site for the Meigma Go repository template."
requires-python = ">=3.14"
dependencies = [
    "mkdocs-material>=9.7.0",
]

[tool.uv]
package = false
